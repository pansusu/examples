# ADK Travel Concierge (Java)

Java translation of `adksamples/travel-concierge` using SequentialAgent.

- Sub-agents: inspiration, planning, booking, pre_trip, in_trip, post_trip

Run:
```bash
mvn -q -DskipTests package
java -jar target/adk-travel-concierge-0.0.1-SNAPSHOT.jar
```
