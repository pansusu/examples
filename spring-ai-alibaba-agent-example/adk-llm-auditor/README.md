# ADK LLM Auditor (Java)

Java translation of `adksamples/llm-auditor` using SequentialAgent.

- Workflow: critic_agent -> reviser_agent

Run:
```bash
mvn -q -DskipTests package
java -jar target/adk-llm-auditor-0.0.1-SNAPSHOT.jar
```
